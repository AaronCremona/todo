$(function() {
  ui.init({
    sortableSelector: "#today, #week",
    connectionSelector: ".connectedSortable"
  });
});


/*

initialize the UI
  make lists sortable and connected

to do object:
  heading
  input new item
  list group

*/
